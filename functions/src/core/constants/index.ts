export enum KEYS {
  ADMIN_KEY = 'ADMIN_KEY'
};

export enum COLLECTION {
  ARTICLES = 'articles'
}

export enum ENV_KEY {
  MODE = 'MODE'
}

export enum ENV_MODE {
  LOCAL = 'LOCAL',
  STAGING = 'STAGING',
  PROD = 'PROD'
}
